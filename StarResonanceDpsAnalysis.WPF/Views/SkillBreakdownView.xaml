<Window x:Class="StarResonanceDpsAnalysis.WPF.Views.SkillBreakdownView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StarResonanceDpsAnalysis.WPF.Views"
    xmlns:ctrl="clr-namespace:StarResonanceDpsAnalysis.WPF.Controls"
        mc:Ignorable="d"
        Title="SkillBreakdownView" 
        Height="450" Width="800"
        Style="{StaticResource TransparentWindow}">

    <!--  WindowChrome: 开启可拉伸边界  -->
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="InfoAndTypeGrid" TargetType="Grid">
            <Setter Property="Height" Value="60" />
            <Setter Property="Background" Value="#FFF" />
        </Style>

        <Style x:Key="ControlButton" TargetType="Button">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Width" Value="30" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Border Background="#EFEFEF" Style="{StaticResource ShadowWindowBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ctrl:Header Title="Skill Breakdown" Grid.Row="0" />

            <Grid Style="{StaticResource InfoAndTypeGrid}" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="--测试名称--" Margin="30,0,0,0" Grid.Column="0"/>
                <TextBlock Text="UID: 123456" Margin="20,0,0,0" Grid.Column="1"/>
                <TextBlock Text="战力: 25000" Margin="20,0,0,0" Grid.Column="2"/>
                <TextBlock Text="等级: 60" Margin="20,0,0,0" Grid.Column="3"/>
                <TextBlock Text="臂章: 19" Margin="20,0,0,0"  Grid.Column="4"/>

                <Border Width="300" CornerRadius="99" Grid.Column="6">
                    
                </Border>
            </Grid>

            <Grid Grid.Row="2">
                
            </Grid>

            <Border Grid.Row="3">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Height" Value="50" />
                        <Setter Property="Background" Value="#FFF" />
                        <Setter Property="CornerRadius">
                            <Setter.Value>
                                <CornerRadius
                                    BottomLeft="{StaticResource MainCornerRadiusDouble}"
                                    BottomRight="{StaticResource MainCornerRadiusDouble}"
                                    TopLeft="0"
                                    TopRight="0" />
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Border.Style>
                
                <Grid Width="100" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    
                    <Button
                        x:Name="Confirm"
                        Grid.Column="0"
                        Command="{Binding ConfirmCommand}"
                        Style="{StaticResource ControlButton}">
                        <Button.Content>
                            <Image Source="{StaticResource ImageOkNormal}" />
                        </Button.Content>
                    </Button>
                    
                    <Button
                        x:Name="Cancel"
                        Grid.Column="1"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource ControlButton}">
                        <Button.Content>
                            <Image Source="{StaticResource ImageCancelNormal}" />
                        </Button.Content>
                    </Button>
                    
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
